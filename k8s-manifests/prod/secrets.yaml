# DO NOT commit real secrets. Use Sealed Secrets or External Secrets Operator.
# This file is a template — populate via CI/CD pipeline.
apiVersion: v1
kind: Secret
metadata:
  name: app-secrets
  namespace: prod
type: Opaque
data:
  JWT_SECRET: REPLACE_WITH_BASE64_ENCODED_STRONG_SECRET
  MONGODB_URI: REPLACE_WITH_BASE64_ENCODED_ATLAS_URI
