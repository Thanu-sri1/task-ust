{{- if .Values.apiGateway.enabled }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Values.apiGateway.name }}
  namespace: {{ .Values.namespace }}
spec:
  replicas: {{ .Values.apiGateway.replicas }}
  selector:
    matchLabels:
      app: {{ .Values.apiGateway.name }}
{{- with .Values.apiGateway.strategy }}
  strategy:
{{ toYaml . | nindent 4 }}
{{- end }}
  template:
    metadata:
      labels:
        app: {{ .Values.apiGateway.name }}
    spec:
{{- with .Values.apiGateway.podSecurityContext }}
      securityContext:
{{ toYaml . | nindent 8 }}
{{- end }}
      containers:
        - name: {{ .Values.apiGateway.name }}
          image: {{ .Values.apiGateway.image }}
          imagePullPolicy: {{ .Values.apiGateway.imagePullPolicy }}
          ports:
            - containerPort: {{ .Values.apiGateway.containerPort }}
{{- with .Values.apiGateway.env }}
          env:
{{ toYaml . | nindent 12 }}
{{- end }}
{{- with .Values.apiGateway.resources }}
          resources:
{{ toYaml . | nindent 12 }}
{{- end }}
{{- with .Values.apiGateway.livenessProbe }}
          livenessProbe:
{{ toYaml . | nindent 12 }}
{{- end }}
{{- with .Values.apiGateway.readinessProbe }}
          readinessProbe:
{{ toYaml . | nindent 12 }}
{{- end }}
---
apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.apiGateway.name }}
  namespace: {{ .Values.namespace }}
spec:
  selector:
    app: {{ .Values.apiGateway.name }}
  ports:
    - port: {{ .Values.apiGateway.servicePort }}
      targetPort: {{ .Values.apiGateway.serviceTargetPort }}
  type: {{ .Values.apiGateway.serviceType }}
{{- end }}
